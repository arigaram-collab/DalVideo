<Window x:Class="DalVideo.Views.CountdownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Countdown"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        KeyDown="OnKeyDown"
        AutomationProperties.Name="녹화 카운트다운">

    <Border Background="#DD1E1E2E" CornerRadius="20" Padding="50,24"
            BorderBrush="#44FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="40" ShadowDepth="4" Opacity="0.6"/>
        </Border.Effect>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock x:Name="CountdownText" Text="3"
                       FontSize="120" FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Opacity="1"/>
            <TextBlock Text="ESC: 취소" FontSize="13" Foreground="#99FFFFFF"
                       HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Border>
</Window>
